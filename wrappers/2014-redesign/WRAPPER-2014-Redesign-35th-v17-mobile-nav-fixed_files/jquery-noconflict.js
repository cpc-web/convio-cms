/* see http://api.jquery.com/jQuery.noConflict/ */

// call no conflict such that the '$' global convenience variable is released.
$.noConflict(false);

//the jQuery global variable should still be available
jQuery.noop();